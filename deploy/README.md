# Déploiement MyBank (prod)

## Prérequis

- VM avec Docker & Docker Compose
- Fichier `deploy/.env` rempli (copie de `.env.example`)

## Déployer

```bash
cd deploy
cp .env.example .env   # puis édite .env
bash deploy.sh
```
